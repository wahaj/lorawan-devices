# This example contains just one end device: vct-insite-4b. It is referenced here in the index.

endDevices:
  # Unique identifier of the end device (lowercase, alphanumeric with dashes, max 36 characters)
  - insite-4b # look in vct-insite-4b.yaml for the end device definition